// @ts-check
import { defineConfig } from 'astro/config';

// Your site is a GitHub "user site" (repo named jgdmagz.github.io),
// so it serves from the root — no `base` path needed.
// https://astro.build/config
export default defineConfig({
  site: 'https://jgdmagz.github.io',
});
